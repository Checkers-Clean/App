import 'package:checker/GameOnline/SalaDeEspera/esperaForm.dart';
import 'package:flutter/material.dart';

class esperaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.black, // Fondo negro del Scaffold
      body: EsperaForm(),
    );
  }
}
